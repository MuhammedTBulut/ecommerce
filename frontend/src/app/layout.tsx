import type { Metadata } from "next";
import "./globals.css";
import { Header } from "@/components/layout/header";
import { Footer } from "@/components/layout/footer";

export const metadata: Metadata = {
  title: "Modern E-Commerce | Premium Online Shopping",
  description: "Discover premium products with our modern e-commerce platform. Fast shipping, secure payments, and exceptional customer service.",
  keywords: "e-commerce, online shopping, premium products, fashion, electronics",
  openGraph: {
    title: "Modern E-Commerce",
    description: "Premium online shopping experience",
    type: "website",
  }
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className="font-sans antialiased min-h-screen flex flex-col">
        <Header />
        <main className="flex-1">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
